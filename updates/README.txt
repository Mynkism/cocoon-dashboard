Drop fresh CSV exports here with the exact same filenames as the /data folder.
Then run: node update.js from the project root.
Files here are temporary — they can be deleted after each successful update.
